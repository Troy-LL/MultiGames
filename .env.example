# PartyKit host the browser connects to for multiplayer (WebSocket).
# Local dev: leave unset (defaults to localhost:1999) and run `npm run dev:party`.
# Production: deploy with `npm run deploy`, then set this in Vercel to your
# PartyKit host, e.g. multiplayer-sudoku.<account>.partykit.dev
VITE_PARTYKIT_HOST=
